# Environment Variables for Multiplayer Dungeon Game
# Copy this file to .env for local development

# Server port (Railway sets this automatically in production)
PORT=2567

# Note: The client automatically detects the environment:
# - localhost: uses ws://localhost:2567
# - production: uses wss://<your-domain> (secure WebSocket)
